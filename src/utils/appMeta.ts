/** Product metadata surfaced in Settings/About. */
export const APP_NAME = 'AI Diagram Copilot';

/** Marketing/product version. Phase 1 delivers the standalone editor (v1.0). */
export const APP_VERSION = '1.0.0';

export const APP_PHASE = 'Phase 1 — Standalone Editor';
